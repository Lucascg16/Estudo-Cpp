#include <stdio.h>

int main(void){
    char nome[30] = "nome";
    char *exemplo = "exemplo";//utilizando em forma de ponteiro, nao precisa colocar o tamanho

    printf (nome);
    printf(exemplo);

    return 0;
}